/**
 * @fileoverview Country Code - Enum for ISO country codes
 * @summary Two-letter country codes for language selection
 * @description
 * Defines ISO 3166-1 alpha-2 country codes used for language selection.
 * These codes are displayed in the language selector dropdown.
 */

export enum CountryCode {
  GreatBritain = 'GB',
  Spain = 'ES',
  Japan = 'JP',
  Italy = 'IT',
}

