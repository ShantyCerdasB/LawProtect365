/**
 * @fileoverview MfaReason - Enum for MFA policy denial reasons
 * @summary Defines MFA policy denial reason codes
 * @description This enum provides type-safe MFA policy denial reason values for
 * error handling and logging.
 */

export enum MfaReason {
  /** MFA is required but not satisfied */
  MFA_REQUIRED = 'MFA_REQUIRED'
}
