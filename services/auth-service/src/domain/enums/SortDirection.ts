/**
 * @fileoverview SortDirection - Enum for sort direction
 * @summary Defines the available sort directions
 * @description This enum defines the sort directions for ordered queries
 * to ensure type safety and consistency.
 */

export enum SortDirection {
  ASC = 'asc',
  DESC = 'desc'
}
