/**
 * @fileoverview AdminIncludeField - Enum for admin user include fields
 * @summary Defines the optional fields to include in admin user responses
 * @description This enum defines the optional fields that can be included
 * in admin user listing responses for enhanced data retrieval.
 */

export enum AdminIncludeField {
  PROFILE = 'profile',
  IDP = 'idp'
}
