/**
 * @file index.ts
 * @summary Envelope types shared across envelope ports
 * @description Exports envelope-related interfaces used by envelope port implementations
 */

export * from "./types.envelope";
