/**
 * @fileoverview PushPriority - Priority enumeration for push notifications
 * @summary Defines priority levels for push notifications
 * @description Enumeration for push notification priority levels used by FCM and APNS.
 */

/**
 * Push notification priority levels
 */
export enum PushPriority {
  NORMAL = 'normal',
  HIGH = 'high',
}

