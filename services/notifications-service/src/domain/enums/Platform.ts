/**
 * @fileoverview Platform - Platform enumeration for push notifications
 * @summary Defines supported mobile platforms
 * @description Enumeration for identifying mobile platforms (Android/iOS)
 * to determine which push notification service to use.
 */

/**
 * Supported mobile platforms for push notifications
 */
export enum Platform {
  ANDROID = 'android',
  IOS = 'ios',
}

